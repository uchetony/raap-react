import React from 'react'
import './PageInfo.scss'

const PageInfo = (props) => {
    return (
       <React.Fragment>
           {props.children}
       </React.Fragment>
    )
}

export default PageInfo
